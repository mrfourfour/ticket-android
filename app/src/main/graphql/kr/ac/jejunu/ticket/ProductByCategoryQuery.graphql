query ProductByCategoryQuery($categorys : String, $area : String) {
    productByCategory(category: $categorys, area: $area) {
        id
        name
        sellerId
        image
        category
        subCategory
        info
        area
        price
        options {
            id
            description
            date
            amout
        }
    }
}

