query ProductByCategoryQuery($categorys : String) {
    productByCategory(category: $categorys) {
        id
        name
        sellerId
        image
        category
        subCategory
        info
        area
        price
        option
    }
}